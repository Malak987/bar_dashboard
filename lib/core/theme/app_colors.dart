import 'package:flutter/material.dart';

/// ألوان مستوحاة من تصميم الداشبورد + لوجو BAR
class AppColors {
  // الألوان الأساسية (Teal من اللوجو)
  static const Color primary = Color(0xFF1FA89A);
  static const Color primaryDark = Color(0xFF0E2A33);
  static const Color primaryLight = Color(0xFF2DBFB0);

  // اللون البرتقالي (من اللوجو)
  static const Color accent = Color(0xFFFF8A5C);

  // خلفيات
  static const Color background = Color(0xFF0A1F26);
  static const Color surface = Color(0xFF12303A);
  static const Color surfaceLight = Color(0xFF1A3D48);
  static const Color cardColor = Color(0xFF153642);

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB8C5CC);
  static const Color textHint = Color(0xFF6B8590);

  // Status
  static const Color success = Color(0xFF22C55E);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
  static const Color info = Color(0xFF3B82F6);

  // Borders
  static const Color border = Color(0xFF1F4452);
}
